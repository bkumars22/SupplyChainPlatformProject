[
#foreach($e in $dp)
    {
        #foreach($k in $e.entrySet())
            "$k.key" : "$k.value"#if( $foreach.hasNext ),#end
                        
        #end
    }
    #if( $foreach.hasNext ),#end
    
#end
]
